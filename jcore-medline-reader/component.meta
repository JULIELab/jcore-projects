{
    "categories": [
        "reader"
    ],
    "description": "Leveraging the JCoRe XML Reader, this project employs a Medline-enabled mapping file to map Medline XML documents to CAS instances. The input is a directory of single XML files, each containing exactly one XML MedlineCitation. The XML format is the one used by NCBI when downloading Medline documents in large batches from the NCBI FTP server. However, the component currently expects single documents instead of whole MedlineCitationSets containing sets of MedlineCitations.",
    "descriptors": [
        {
            "category": "reader",
            "location": "de.julielab.jcore.reader.xml.desc.jcore-medline-reader"
        }
    ],
    "exposable": true,
    "group": "general",
    "maven-artifact": "jcore-medline-reader",
    "name": "JCoRe Medline Reader"
}
